# PromixFlame PSSG Editor

Редактор файлов PSSG для модификации графических ресурсов игр серии F1 (F1 2012 и аналогичных).

## О проекте

**PSSG Editor** — это WPF-приложение на .NET 8, предназначенное для чтения, редактирования и сохранения файлов формата PSSG (Processed Scene Structure Graph). Этот формат используется в играх Codemasters F1 для хранения 3D-моделей, текстур и других графических ресурсов.

## Возможности

### Работа с файлами
- ✅ Открытие файлов PSSG и ENS (включая поддержку gzip-сжатия)
- ✅ Сохранение изменений в формате PSSG
- ✅ **Экспорт структуры нодов в JSON** для анализа и обмена информацией
- ✅ Экспорт выбранной ноды и её поддерева в отдельный JSON файл
- ✅ Автоматическая загрузка файла при запуске через аргументы командной строки
- ✅ Прогресс-бар загрузки файлов

### Редактирование структуры
- 🌳 Навигация по дереву узлов PSSG
- 📝 Просмотр и редактирование атрибутов узлов
- 🔢 Поддержка различных типов данных:
  - Целые числа (uint8, uint16, uint32)
  - Числа с плавающей точкой (float)
  - Строки (UTF-8 с префиксом длины)
  - Массивы float (для Transform и BoundingBox)
  - Raw данные в hex-формате
- 📊 Сортировка атрибутов по имени или значению
- 💾 Ленивая загрузка больших данных для оптимизации памяти

### Работа с текстурами
- 🖼️ Просмотр списка всех текстур в файле
- 👁️ Превью текстур в формате DDS
- 📥 Импорт текстур из DDS файлов
- 📤 Экспорт текстур в DDS формат
- 📂 Массовый импорт текстур из папки
- 💾 Массовый экспорт всех текстур
- ➕ Добавление новых текстур
- ❌ Удаление существующих текстур
- 🎨 Поддержка форматов: DXT1, DXT3, DXT5, ARGB

### Интерфейс
- 📑 Двухпанельный интерфейс с вкладками:
  - **Nodes** — дерево структуры, атрибуты и raw data
  - **Textures** — список текстур с превью
- 🎯 Удобное редактирование значений с сохранением позиции скролла
- ⌨️ Горячие клавиши (Enter для сохранения, Escape для отмены)
- 📊 Статистика файла: количество узлов, мешей и текстур
- 📈 Прогресс-бар с индикацией "Loading..." при загрузке файлов

## Архитектура

### Основные компоненты

```
PSSG Editor/
├── PSSGFormat.cs                   # Парсинг и запись формата PSSG
│   ├── PSSGNode                   # Узел дерева (атрибуты + дети)
│   ├── PSSGSchema                 # Схема сопоставления ID ↔ имена
│   ├── PSSGParser                 # Чтение файлов PSSG
│   └── PSSGWriter                 # Запись файлов PSSG
│
├── MainWindow.xaml                 # Интерфейс приложения
├── MainWindow.xaml.cs              # Главное окно
├── MainWindow.MenuHandlers.cs      # Обработка меню
├── MainWindow.EditingHandlers.cs   # Редактирование данных
├── MainWindow.TextureHandlers.cs   # Работа с текстурами
├── MainWindow.TreeDisplay.cs       # Отображение дерева
├── MainWindow.Converters.cs        # Конвертация данных
├── MainWindow.VisualHelpers.cs     # Вспомогательные методы UI
│
├── AttributeItem.cs                # Модель атрибута для DataGrid
├── JsonExporter.cs                 # Экспорт структуры в JSON
└── App.xaml.cs                    # Точка входа приложения
```

## Формат PSSG — Техническая спецификация

PSSG (Processed Scene Structure Graph) — проприетарный бинарный формат Codemasters для хранения 3D-сцен, текстур, материалов, анимаций и других игровых ресурсов.

### Структура файла

```
[Header]           # 8 bytes
[Schema Section]   # Variable size
[Node Tree]        # Variable size, recursive structure
```

### 1. Header (8 байт)

| Offset | Size | Type    | Name           | Description                          |
|--------|------|---------|----------------|--------------------------------------|
| 0x00   | 4    | char[4] | Signature      | Всегда "PSSG" (0x50 0x53 0x53 0x47) |
| 0x04   | 4    | uint32  | FileDataLength | Размер данных после этого поля (BE)  |

**FileDataLength** = размер файла - 8 байт

### 2. Schema Section

Schema описывает маппинг между числовыми ID и строковыми именами узлов/атрибутов.

#### 2.1. Schema Header (8 байт)

| Offset | Size | Type   | Name          | Description                               |
|--------|------|--------|---------------|-------------------------------------------|
| 0x08   | 4    | uint32 | AttrInfoCount | Общее кол-во атрибутов во всех узлах (BE) |
| 0x0C   | 4    | uint32 | NodeInfoCount | Количество типов узлов (BE)               |

#### 2.2. Node Info Block (повторяется NodeInfoCount раз)

Для каждого типа узла:

| Field           | Type   | Description                                |
|-----------------|--------|--------------------------------------------|
| NodeId          | uint32 | Уникальный ID типа узла (BE)               |
| NodeNameLength  | uint32 | Длина имени узла в байтах (BE)            |
| NodeName        | byte[] | Имя узла в UTF-8 (без null-терминатора)   |
| AttributeCount  | uint32 | Количество атрибутов для этого узла (BE)  |

Затем для каждого атрибута узла:

| Field               | Type   | Description                         |
|---------------------|--------|-------------------------------------|
| AttributeId         | uint32 | Уникальный ID атрибута (BE)         |
| AttributeNameLength | uint32 | Длина имени атрибута в байтах (BE)  |
| AttributeName       | byte[] | Имя атрибута в UTF-8                |

### 3. Node Tree (рекурсивная структура)

После Schema идёт дерево узлов. Каждый узел состоит из заголовка, атрибутов и payload (дочерние узлы или raw data).

#### 3.1. Node Header (12 байт)

| Offset | Size | Type   | Name          | Description                                |
|--------|------|--------|---------------|--------------------------------------------|
| +0     | 4    | uint32 | NodeId        | ID типа узла (ссылка на Schema) (BE)      |
| +4     | 4    | uint32 | NodeSize      | Размер данных ПОСЛЕ этого поля (BE)       |
| +8     | 4    | uint32 | AttrBlockSize | Размер блока атрибутов (BE)               |

## Системные требования

- **ОС:** Windows 10/11
- **Framework:** .NET 8.0 Runtime (Desktop)
- **Архитектура:** x64

## Сборка проекта

### Требования для разработки
- Visual Studio 2022 (или новее) с компонентом ".NET Desktop Development"
- .NET 8.0 SDK

### Команды сборки

```bash
# Debug сборка
dotnet build

# Release сборка
dotnet build -c Release

# Публикация single-file (без встроенного runtime)
dotnet publish -c Release -r win-x64 --self-contained false /p:PublishSingleFile=true
```

Исполняемый файл будет создан в:
```
bin/Release/net8.0-windows/win-x64/publish/PromixFlame PSSG Editor.exe
```

## Использование

### Запуск
1. Запустите `PromixFlame PSSG Editor.exe`
2. Выберите **File → Open** и откройте файл `.pssg` или `.ens`
3. Или перетащите файл на exe для автоматической загрузки

### Редактирование атрибутов
1. В дереве **Nodes** выберите нужный узел
2. В правой панели отобразятся атрибуты
3. Дважды кликните на значение в колонке **Value**
4. Измените значение и нажмите **Enter** (или **Escape** для отмены)

### Работа с текстурами
1. Перейдите на вкладку **Textures**
2. Выберите текстуру из списка для превью
3. Используйте контекстное меню (ПКМ) для:
   - Import Texture — заменить выбранную текстуру
   - Export Texture — сохранить как DDS
   - New Texture — добавить новую из DDS
   - Delete — удалить текстуру

### Массовые операции
- **Textures → Import Folder** — импорт всех DDS из папки (по имени файла)
- **Textures → Export All Textures** — экспорт всех текстур в выбранную папку

### Экспорт в JSON

**File → Export to JSON** — экспорт всей структуры файла в JSON

Эта функция позволяет экспортировать полную структуру PSSG файла в читаемый JSON формат. Это полезно для:

- 🔍 **Анализа структуры** — изучения иерархии нодов, атрибутов и данных
- 🤖 **Обработки через AI/LLM** — передачи информации о файле Claude или ChatGPT
- 📝 **Документирования** — создания описаний формата PSSG
- 🔧 **Автоматизации** — создания скриптов для работы с PSSG
- 👥 **Обмена информацией** — обсуждения структуры файла с другими разработчиками

**File → Export Selected Node to JSON** — экспорт выбранной ноды

Позволяет экспортировать только выбранную ноду и все её дочерние ноды.

## Поддерживаемые форматы данных

### Атрибуты
- **uint8/16/32** — беззнаковые целые числа
- **float** — числа с плавающей точкой (IEEE 754)
- **string** — UTF-8 строки с 4-байтовым префиксом длины
- **hex** — произвольные бинарные данные

### Текстуры
- **DXT1** — сжатие без альфа-канала (8 байт на блок 4×4)
- **DXT3** — сжатие с простым альфа-каналом (16 байт на блок)
- **DXT5** — сжатие с интерполированным альфа-каналом (16 байт на блок)
- **ARGB** — несжатое 32-битное RGBA (4 байта на пиксель)

## Известные ограничения

- ⚠️ Редактирование структуры дерева (добавление/удаление узлов) доступно только для текстур
- ⚠️ Поддержка только чтения DDS; для создания текстур используйте внешние инструменты

## Лицензия

Проект создан для моддинга игр F1. Используется на свой риск.

## Автор

**PromixFlame** - разработка и дизайн

---

*PSSG Editor v1.0 — For F1 2012 modding community*
